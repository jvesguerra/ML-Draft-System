🛠️ Bug Fixes
Fix Screen Dismissal: Resolve the issue where the screen closes/clears unexpectedly when an enemy hero is entered.

⚙️ Automation & Logic
Role Auto-Population: Simplify the user flow by allowing users to select only the Lane; the Role should be fetched automatically via the API.

Updated Draft Flow: Implement a "5-Hero Ban Phase" that must be completed before the picking phase begins.

Counter Logic: Rename and pivot the "Ban Recommendation" engine to focus on Counter Suggestions based on opponent picks.

🎨 UI/UX & Search
Predictive Search (Typeahead): Implement a "Guess" or "Autocomplete" feature that suggests heroes in real-time as the user types.

Optimized Rendering: Ensure the interface handles real-time updates smoothly without flickering or "full-page" reloads as data is entered.

Alphabetical Sorting: Force the hero list to display in A-Z order by default for easier navigation.